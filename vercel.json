{
  "version": 2,
  "buildCommand": "cd bearmarket && npm install && npm run build",
  "outputDirectory": "bearmarket/build",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/price.py": {
      "runtime": "python3.12"
    }
  }
}
